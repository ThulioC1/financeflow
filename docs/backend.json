{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Representa um usuário do aplicativo FinanceFlow.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Identificador único para o usuário (Firebase Auth UID)."
        },
        "nome": {
          "type": "string",
          "description": "O nome completo do usuário."
        },
        "email": {
          "type": "string",
          "description": "O endereço de e-mail do usuário.",
          "format": "email"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicando quando a conta do usuário foi criada.",
          "format": "date-time"
        }
      },
      "required": [
        "uid",
        "nome",
        "email",
        "createdAt"
      ]
    },
    "Income": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Receita",
      "type": "object",
      "description": "Representa uma entrada de receita para um usuário.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Identificador único para a entrada de receita.",
          "format": "uuid"
        },
        "userId": {
          "type": "string",
          "description": "Referência ao usuário que possui esta receita."
        },
        "valor": {
          "type": "number",
          "description": "O valor da receita."
        },
        "tipo": {
          "type": "string",
          "description": "O tipo de receita (e.g., mensal, quinzena 1, quinzena 2, extra).",
          "enum": [
            "mensal",
            "quinzena 1",
            "quinzena 2",
            "extra"
          ]
        },
        "mesReferencia": {
          "type": "string",
          "description": "O mês a que a receita se refere (AAAA-MM)."
        },
        "status": {
          "type": "string",
          "description": "O status da receita (e.g., pendente, pago)."
        },
        "dataRecebimento": {
          "type": "string",
          "description": "A data em que a receita foi recebida.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicando quando a entrada de receita foi criada.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "valor",
        "tipo",
        "mesReferencia",
        "status",
        "createdAt"
      ]
    },
    "Expense": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Despesa",
      "type": "object",
      "description": "Representa uma entrada de despesa para um usuário.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Identificador único para a entrada de despesa.",
          "format": "uuid"
        },
        "userId": {
          "type": "string",
          "description": "Referência ao usuário que possui esta despesa."
        },
        "descricao": {
          "type": "string",
          "description": "Uma descrição da despesa."
        },
        "valor": {
          "type": "number",
          "description": "O valor da despesa."
        },
        "categoria": {
          "type": "string",
          "description": "A categoria da despesa."
        },
        "recorrente": {
          "type": "boolean",
          "description": "Indica se a despesa é recorrente."
        },
        "mesReferencia": {
          "type": "string",
          "description": "O mês a que a despesa se refere (AAAA-MM)."
        },
        "status": {
          "type": "string",
          "description": "O status da despesa (e.g., pago, pendente)."
        },
        "dataPagamento": {
          "type": "string",
          "description": "A data em que a despesa foi paga.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicando quando a entrada de despesa foi criada.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "descricao",
        "valor",
        "categoria",
        "recorrente",
        "mesReferencia",
        "status",
        "createdAt"
      ]
    },
    "Balance": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Saldo",
      "type": "object",
      "description": "Representa o saldo de um usuário para um mês específico.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Identificador único para a entrada de saldo.",
          "format": "uuid"
        },
        "userId": {
          "type": "string",
          "description": "Referência ao usuário que possui este saldo."
        },
        "mesReferencia": {
          "type": "string",
          "description": "O mês a que o saldo se refere (AAAA-MM)."
        },
        "saldoInicial": {
          "type": "number",
          "description": "O saldo inicial do mês."
        },
        "totalReceitas": {
          "type": "number",
          "description": "O total de receitas do mês."
        },
        "totalDespesas": {
          "type": "number",
          "description": "O total de despesas do mês."
        },
        "saldoFinal": {
          "type": "number",
          "description": "O saldo final do mês."
        }
      },
      "required": [
        "id",
        "userId",
        "mesReferencia",
        "saldoInicial",
        "totalReceitas",
        "totalDespesas",
        "saldoFinal"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Armazena informações do perfil do usuário. A propriedade baseada no caminho garante que apenas o usuário possa acessar seu perfil.",
          "params": [
            {
              "name": "userId",
              "description": "O identificador único do usuário."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/incomes/{incomeId}",
        "definition": {
          "entityName": "Income",
          "schema": {
            "$ref": "#/backend/entities/Income"
          },
          "description": "Armazena entradas de receita para um usuário específico. A propriedade baseada no caminho garante que apenas o usuário possa acessar seus dados de receita.",
          "params": [
            {
              "name": "userId",
              "description": "O identificador único do usuário."
            },
            {
              "name": "incomeId",
              "description": "O identificador único da entrada de receita."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/expenses/{expenseId}",
        "definition": {
          "entityName": "Expense",
          "schema": {
            "$ref": "#/backend/entities/Expense"
          },
          "description": "Armazena entradas de despesa para um usuário específico. A propriedade baseada no caminho garante que apenas o usuário possa acessar seus dados de despesa.",
          "params": [
            {
              "name": "userId",
              "description": "O identificador único do usuário."
            },
            {
              "name": "expenseId",
              "description": "O identificador único da entrada de despesa."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/balances/{balanceId}",
        "definition": {
          "entityName": "Balance",
          "schema": {
            "$ref": "#/backend/entities/Balance"
          },
          "description": "Armazena informações de saldo para um usuário específico. A propriedade baseada no caminho garante que apenas o usuário possa acessar seus dados de saldo.",
          "params": [
            {
              "name": "userId",
              "description": "O identificador único do usuário."
            },
            {
              "name": "balanceId",
              "description": "O identificador único da entrada de saldo."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to ensure data isolation between users and efficient querying of financial data. Each user's data (incomes, expenses, and balances) is stored under their respective user ID, enabling secure and straightforward security rules. The path-based ownership pattern (`/users/{userId}/incomes/{incomeId}`, `/users/{userId}/expenses/{expenseId}`, `/users/{userId}/balances/{balanceId}`) guarantees that users can only access their own data, adhering to the application's security requirements.\\n\\n**Authorization Independence:**\\n\\n*   The structure inherently achieves Authorization Independence. Each document (income, expense, balance) is stored within a path specific to a user (`/users/{userId}`). This eliminates the need for `get()` calls in security rules to validate ownership. The security rules can directly verify `request.auth.uid == userId` to grant access, making the authorization process atomic and independent of other documents.\\n\\n**QAPs Support:**\\n\\n*   The structure supports secure `list` operations because each user's data is segregated into its own subcollection. Security rules can allow listing operations based on the `userId` in the path, preventing unauthorized access to other users' data.\\n*   The segregation of data into separate collections (`incomes`, `expenses`, `balances`) also simplifies security rules by ensuring that all documents within a collection share the same security requirements. This avoids the need for complex rule logic based on document content.\\n\\nThis design promotes clarity, security, and scalability, aligning with the application's requirements for a personal finance management system."
  }
}
